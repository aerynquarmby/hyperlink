/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['source.unsplash.com', 'cryptoicons.org', 'raw.githubusercontent.com', 'picsum.photos'],
    },
  };
  
  module.exports = nextConfig;
  